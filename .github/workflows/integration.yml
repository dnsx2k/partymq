name: Integration tests v2
on:
  push:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: docker:latest
      options: --privileged
    steps:
      - name: Tests
        with:
          file: docker-compose.yml
          run: docker compose up